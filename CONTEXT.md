# Governed BI Agent

本上下文描述用户可配置业务语义、发起数据分析并核查分析证据时使用的统一领域语言。它确保产品、规格、代码和测试对同一概念使用同一个名称。

## 组织与语义

**Workspace（工作区）**：
一组用户、数据源、业务语义和访问策略共同归属的治理边界。
_避免使用_：Tenant、租户、项目空间

**Semantic Package（语义包）**：
某个工作区内可一起校验、发布和追溯的一组业务语义定义，包括模型、指标、维度、关系、词汇、策略和 Approved Example。
_避免使用_：领域包、知识包、语义模型

**Semantic Version（语义版本）**：
语义包在一次发布时形成的不可变快照，是分析结果解释和重放所依据的版本。
_避免使用_：配置版本、模型版本

**Semantic Member（语义成员）**：
语义包中具有稳定标识、可被查询或治理规则引用的定义，例如指标、维度和模型。
_避免使用_：字段、属性（用于泛指语义定义时）

**Approved Example（已审核示例）**：
经人工确认的业务问题、分析意图与 SemanticQuery 的对应样例，可用于检索、评估和回归。
_避免使用_：Few-shot、样例 SQL、训练样本

## 分析

**SemanticQuery（语义查询）**：
使用语义成员标识描述指标、维度、过滤、时间范围、比较、排序和限制的结构化分析请求。
_避免使用_：SQL、查询文本、Agent SQL

**AnalysisRun（分析运行）**：
从一次用户问题开始，经过澄清、规划、执行直到成功、拒绝或失败的可追溯分析实例。
_避免使用_：Chat、Task、Session

**AnalysisContext（分析上下文）**：
多轮分析中当前已确认的 SemanticQuery、语义版本和必要假设，后续追问以它为基准。
_避免使用_：聊天上下文、Memory

**Query Patch（查询变更）**：
相对于当前 AnalysisContext，对维度、过滤、时间范围、比较或排序所做的结构化增删改。
_避免使用_：Prompt Patch、重写查询

**Artifact（分析产物）**：
AnalysisRun 产生的可独立查看和核查的结构化结果，例如 SemanticQuery、数据表、图表、洞察、警告和查询说明。
_避免使用_：消息、回答（用于泛指结构化结果时）

**VerifiedFacts（可验证事实）**：
从查询结果确定性推导且能够回算的数据事实，是业务洞察引用数字和结论的证据。
_避免使用_：模型结论、AI 洞察、摘要数据

**ChartIntent（图表意图）**：
对图表用途、使用字段和视觉编码的受约束描述，不包含可执行的展示代码。
_避免使用_：图表配置、ECharts Option

**AgentEvent（Agent 事件）**：
AnalysisRun 进行过程中发布的类型化状态或产物变化，是界面观察运行进展的最小单位。
_避免使用_：Token、消息块、日志

## 治理

**SemanticChangeProposal（语义变更提案）**：
用户或 Agent 对正式语义定义提出的待审核修改；提案本身不会改变已发布语义。
_避免使用_：自动学习、在线修改、语义补丁

**Draft（草稿）**：
仍可编辑且尚不能用于正式分析的语义包状态。
_避免使用_：未发布版本、临时配置

**Published（已发布）**：
已经审核通过、可用于正式分析且内容不可变的语义版本状态。
_避免使用_：Active、生产配置

**Deprecated（已弃用）**：
不再推荐用于新分析、但仍保留以支持历史追溯和兼容的语义成员或版本状态。
_避免使用_：Deleted、Disabled
